import React, { createContext, useState } from 'react'
import './styles.sass';

interface MenuManagerProps {
  
}
 
 
export const MenuManager: React.FC<MenuManagerProps> = (props) => {
  
  return (
    <></>
  )
}