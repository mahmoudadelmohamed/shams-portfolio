interface Image {
  id: number;
  images: string;
}
interface LocalGallery {
  id: number;
};
interface LocalProjects {
  id: number;
};
interface PeopleRecommendMe {
  description: string;
  imageUrl: string;
  job: string;
  name: string;
}